#include <iostream>

int main() {
    freopen("generator.input", "r", stdin);
    while ((tmp = getchar()) != EOF) {
        std::cout << tmp;
    }
//    std::cout << sizeof(char) << ' ' << sizeof(int8_t) << '\n';
//    for (int i = 0; i < 1; ++i)
//        std::cout << (int8_t(EOF));
//    std::cout << "Hello, World!" << std::endl;
    return 0;
}